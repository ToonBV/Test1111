import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
      primary: {
        main: '#143c6d',
      },
      secondary: {
        main: '#FFFFFF',
      },
      background: {
        default: '#f0f0f0',
      },
    },
    typography: {
    fontFamily: 'Itim, cursive', // ใช้แบบอักษร Roboto
    

  },
  });

  export default theme;